coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: 80%
        informational: true

flag_management:
  default_rules:
    - flags:
        - linux
      paths:
        - "**"

ignore:
  - "bazel-*/**"
  - "external/**"
  - "third_party/**"
